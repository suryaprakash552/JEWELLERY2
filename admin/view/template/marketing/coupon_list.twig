<form id="form-coupon" method="post" data-oc-toggle="ajax" data-oc-load="{{ action }}" data-oc-target="#coupon">
  <div class="table-responsive">
    <table class="table table-bordered table-hover">
      <thead>
        <tr>
          <th class="text-center" style="width: 1px;"><input type="checkbox" onclick="$('input[name*=\'selected\']').prop('checked', $(this).prop('checked'));" class="form-check-input"/></th>
          <th class="text-center"><a href="{{ sort_name }}"{% if sort == 'name' %} class="{{ order|lower }}"{% endif %}>{{ column_name }}</a></th>
          <th class="text-center"><a href="{{ sort_code }}"{% if sort == 'code' %} class="{{ order|lower }}"{% endif %}>{{ column_code }}</a></th>
          <th class="text-center"><a href="{{ sort_discount }}"{% if sort == 'discount' %} class="{{ order|lower }}"{% endif %}>{{ column_discount }}</a></th>
          <th class="text-center"><a href="{{ sort_minimum_total }}"{% if sort == 'minimum_total' %} class="{{ order|lower }}"{% endif %}>Minimum Bill</a></th>
          <th class="text-center d-none d-lg-table-cell"><a href="{{ sort_date_start }}"{% if sort == 'date_start' %} class="{{ order|lower }}"{% endif %}>{{ column_date_start }}</a></th>
          <th class="text-center d-none d-lg-table-cell"><a href="{{ sort_date_end }}"{% if sort == 'date_end' %} class="{{ order|lower }}"{% endif %}>{{ column_date_end }}</a></th>
          <th class="text-center col-1">{{ column_action }}</th>
        </tr>
      </thead>
      <tbody>
        {% if coupons %}
          {% for coupon in coupons %}
            <tr{% if not coupon.status %} class="table-active opacity-50"{% endif %}>
              <td class="text-center"><input type="checkbox" name="selected[]" value="{{ coupon.coupon_id }}" class="form-check-input"/></td>
              <td class="text-center">{{ coupon.name }}</td>
              <td class="text-center">{{ coupon.code }}</td>
              <td class="text-center">{{ coupon.discount }}</td>
              <td class="text-center">{{ coupon.minimum_total }}</td>
              <td class="text-center d-none d-lg-table-cell">{{ coupon.date_start }}</td>
              <td class="text-center d-none d-lg-table-cell">{{ coupon.date_end }}</td>
              <td class="text-center"><a href="{{ coupon.edit }}" data-bs-toggle="tooltip" title="{{ button_edit }}" class="btn btn-custom-edit"><i class="fa-solid fa-pencil"></i>Edit</a></td>
            </tr>
          {% endfor %}
        {% else %}
          <tr>
            <td class="text-center" colspan="8">{{ text_no_results }}</td>
          </tr>
        {% endif %}
      </tbody>
    </table>
  </div>
  <div class="row">
    <div class="col-sm-6 text-start">{{ results }}</div>
    <div class="col-sm-6 text-end">{{ pagination }}</div>
  </div>
</form>
