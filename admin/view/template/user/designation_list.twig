<form id="form-designation" method="post" data-oc-toggle="ajax" data-oc-load="{{ action }}" data-oc-target="#designation">
  <div class="table-responsive">
    <table class="table table-bordered table-hover">
      <thead>
        <tr align="center">
          <th class="text-center" style="width: 5px;"><input type="checkbox" onclick="$('input[name*=\'selected\']').prop('checked', $(this).prop('checked'));" class="form-check-input"/></th>
          <th><a href="{{ sort_name }}"{% if sort == 'name' %} class="{{ order|lower }}"{% endif %}>{{ column_name }}</a></th>
          <th class="text-center col-1" align="center">{{ column_action }}</th>
        </tr>
      </thead>
      <tbody align="center">
        {% if designations %}
          {% for designation in designations %}
            <tr align="center">
              <td class="text-center"><input type="checkbox" name="selected[]" value="{{ designation.designation_id }}" class="form-check-input"/></td>
              <td>{{ designation.name }}</td>
              <td class="text-center"><a href="{{ designation.edit }}" align="center" data-bs-toggle="tooltip" title="{{ button_edit }}" class="btn btn-custom-edit"><i class="fa-solid fa-pencil"></i>Edit</a></td>
            </tr>
          {% endfor %}
        {% else %}
          <tr>
            <td class="text-center" colspan="2">{{ text_no_results }}</td>
          </tr align="center">
        {% endif %}
      </tbody>
    </table>
  </div>
  <div class="row">
    <div class="col-sm-6 text-start">{{ results }}</div>
    <div class="col-sm-6 text-end">{{ pagination }}</div>
  </div>
</form>
